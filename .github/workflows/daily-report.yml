name: 每日数据搜集报告

on:
  schedule:
    # 每天早上 8:00 (UTC) 运行
    - cron: '0 8 * * *'
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.10'

permissions:
  contents: read
  issues: write

jobs:
  data-collection:
    name: 数据搜集
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: 安装依赖
        run: |
          pip install requests beautifulsoup4 pandas numpy

      - name: 运行 Instagram 竞品数据搜集
        run: |
          python scrapers/instagram.py

      - name: 运行新闻数据搜集
        run: |
          python scrapers/news.py

      - name: 运行趋势数据搜集
        run: |
          python scrapers/trends.py

      - name: 上传搜集的数据
        uses: actions/upload-artifact@v4
        with:
          name: daily-data-${{ github.event_name }}
          path: scrapers/output/
          retention-days: 30

  analysis:
    name: 数据分析
    needs: data-collection
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: 安装依赖
        run: |
          pip install requests beautifulsoup4 pandas numpy

      - name: 下载搜集的数据
        uses: actions/download-artifact@v4
        with:
          name: daily-data-${{ github.event_name }}

      - name: 运行数据分析
        run: |
          python analysis/daily_analysis.py

      - name: 生成报告
        run: |
          python reports/generate_report.py

      - name: 上传报告
        uses: actions/upload-artifact@v4
        with:
          name: daily-report-${{ github.event_name }}
          path: reports/output/
          retention-days: 90

  notification:
    name: 发送通知
    needs: analysis
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 创建 GitHub Issue 记录
        uses: actions/github-script@v7
        with:
          script: |
            const today = new Date().toISOString().split('T')[0];
            const reportUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;

            const title = `📊 每日数据报告 - ${today}`;

            const body = `## 数据搜集摘要

            ✅ 数据搜集完成
            - Instagram 竞品数据已更新
            - 行业新闻已搜集
            - 趋势分析已完成

            ## 查看详细报告
            📊 [GitHub Actions Artifacts](${reportUrl}/artifacts)
            🔍 [仓库页面](https://github.com/${context.repo.owner}/${context.repo.repo})

            ## Instagram 竞品数据
            - @heathceramics (现代建筑陶瓷)
            - @keramicstudio (温暖手作陶瓷)
            - @juliettemcinty (自然有机陶瓷)
            - @thecupcollection (杯具专门店)

            ## 新闻数据
            - Etsy Blog
            - Crafts Magazine

            ## 趋势数据
            - handmade ceramics
            - pottery art
            - tea ceremony
            - ceramic home decor
            - teaware

            ## 需要关注
            - 新发现的竞品活动
            - 热门内容的发布频率
            - 趋势关键词的变化

            ---
            *本报告由 GitHub Actions 自动生成*
            *生成时间：${today}*
            *运行ID：${context.runId}*
            `;

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: title,
              body: body,
              labels: ['daily-report', 'automated']
            }).catch(error => {
              console.error('创建 Issue 失败:', error);
            });
